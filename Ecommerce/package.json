{
  "name": "ecommerce",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "vite preview"
  },
  "dependencies": {
    "swiper": "^11.2.5"
  },
  "devDependencies": {
    "vite": "^4.0.0",         // Or latest version you use
    "react": "^18.0.0",       // Add if React is used
    "react-dom": "^18.0.0",   // Add if React is used
    "@vitejs/plugin-react": "^4.0.0" // Only for React + Vite projects
  }
}
